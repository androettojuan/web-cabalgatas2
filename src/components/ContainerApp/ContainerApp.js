import React from "react";
import { StyledContainerApp } from "./ContainerApp.styles";

const ContainerApp = ({ children }) => {
  return <StyledContainerApp>{children}</StyledContainerApp>;
};

export default ContainerApp;
